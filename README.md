
```yaml
Convert an MP4 to an optimized GIF using palette generation

Usage: mp42gif [OPTIONS] <INPUT> <OUTPUT>

Arguments:
  <INPUT>   Input MP4 file
  <OUTPUT>  Output GIF file

Options:
      --fps <FPS>      Frames per second [default: 15]
      --scale <SCALE>  Scale factor (e.g., 0.5 for half size) [default: 0.4]
  -h, --help           Print help
  -V, --version        Print version
```
